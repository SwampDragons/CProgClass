#include <stdio.h>
#include <stdlib.h>

int main ()

{
    int num1;
    double result;
   
    result = 0.0;

    printf ("calculator is on.\n Enter an operator and a number separated by a space.\n");
    printf ("result = %f \n", &result);
   
    /*
    scanf ("%f %f", &operator, &num1);
    if (operator = + )
        print ("%f + %f is %f", result, num1, result = result + num1) 
    return ( operator constant);
    */
    
    return 0;

}

